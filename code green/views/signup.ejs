<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>EcoSignup | Join the Movement</title>
  <link rel="stylesheet" href="/css/signup.css" />

</head>
<body>
  <div class="bg-gradient"></div>
  <div class="sparkles"></div>

  <div class="container glass-card animate-in">
    <h2>Join the Green Revolution</h2>
    <p>Create your account to track your eco impact 🍃</p>
    <br>
    <form class="signup-form" method="POST" action="/signup">
      <input type="text" name="name" placeholder="Full Name" required />
      <input type="email" name="email" placeholder="Email" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Sign Up</button>
      <div class="footer-text">
        <p>Already have an account? <a href="/login">Login</a></p>
      </div>
    </form>
    <% if (flash.error) { %><div class="flash error"><%= flash.error[0] %></div><% } %>
    <% if (flash.success) { %><div class="flash success"><%= flash.success[0] %></div><% } %>
  </div>
</body>
</html>
